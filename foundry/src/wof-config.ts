/** The build-time system data (virtual:wof-config), typed by the builder that produces it. */
import raw from 'virtual:wof-config';
import type { WofConfig } from '../tools/config-data.ts';

const config = raw as WofConfig;
export default config;
