# Rewrite issues

Disagreements and unclear rules found while rewriting pages. The page follows the tables where they disagree with chapter prose; nothing in `docs/rules/` or `data/` was edited.

## The Rules of Play (`site/src/content/rules/rules-of-play.mdx`)

1. **Who cannot be Helped.** `docs/rules/01-core-rules.md` 268 names the Death Roll's and the passive roll's exception rows as excluding Help, but not the performance roll's. `data/core/bonus-dice-sources.yaml` (help, `applies_to`) and `data/core/dice-pool.yaml` (performance-roll, `bonus_sources_excluded`) exclude Help from it too. Page follows the tables: Help requirements and the Bonus Dice table name all three rolls.
2. **Stress triggers beyond the chapter.** `01-core-rules.md` 221-236 lists the core triggers, defers Chapter 7 amounts, and omits two rows. `data/core/stress-changes.yaml` gives the amounts (camp relief 1, Skirmish end 1, Downtime 2) and adds `graduation-exam-ends` (all Stress) and Fear contagion under `named-gain`. Page renders the table, so all of these appear.
3. **Unclear: a Push that "would re-roll no die".** `01-core-rules.md` 197 and `stress-changes.yaml` (cover, `condition`) say such a Push cannot be Covered. An uncovered Push always adds and rolls a new Stress Die (159), so the test only works if it means "no existing base die or Stress Die would be rolled again". Page keeps the source wording ("because every base die and Stress Die already shows 6").
4. **Voice guide conflict.** `site/design/voice-guide.md` asks for "Field report" callouts but bans the word "field". Page text avoids "field": examples use the report component with the default "Example" eyebrow, and quotes cite "Survey Corps manual".
5. **DiceKey retypes a table.** `site/src/components/DiceKey.astro` hardcodes the die-type rows instead of rendering `dice-pool.yaml` `die_types`. The wording agrees with the table today. It also labels Titan Dice "Titan Attack Dice", which `CONTEXT.md` does not use as a term. Not changed in this rewrite.
6. **Glossary pop-up uses an avoid word.** The existing `Titan` entry in `site/src/lib/glossary.ts` calls a Titan "a mindless giant"; `CONTEXT.md` lists "giant" under Avoid. This page does not attach a pop-up to "Titan" for that reason. Not changed in this rewrite.
7. **The reach of Circumstances reads backwards in the table.** `data/core/circumstances.yaml` (`applies_to`) says "Every attribute roll a soldier or a Squadmate makes, except the rolls under never: a called roll, an action from the Action Catalog, a Reaction, and every roll a rule calls for." Read literally, the list after the colon is the exception, which would exempt every roll there is. `never` lists only the Death Roll, the performance roll, and the Graduation Exam's Trials, and `01-core-rules.md` 105 gives the same four kinds as the rolls that *do* take a step. Page follows `never` and the chapter: the list is what Circumstances reach.
8. **The exceptions table cannot show the passive roll's real exceptions.** `dice-pool.yaml` (passive-roll) excludes no pool component, so the "Leaves out" cell reads "Nothing". What actually sets the roll apart is that it is made out of sight, takes only the `circumstances` Bonus Dice source, and gives no Stress Response on a Stress Die showing 1. The page states those in prose under *Passive rolls*; the table shows only the attribute, Help, and Push columns.
9. **A Bonus Dice source whose dice depend on a named step.** `bonus-dice-sources.yaml` (circumstances) gives `dice_per_unit` as a map of step to dice rather than a number, unlike every other source. `site/src/lib/core-tables.ts` renders that case by reading the step names from `circumstances.yaml`, so the cell reads "1 at Easy, 2 at Routine, or 3 at Effortless". A later source shaped this way needs no new code; a source with an unknown step id fails the build.
10. **The failure menu is rendered, not retyped.** The closed menu of what a failed called roll costs is `dice-pool.yaml` (`called_roll`, `failure_menu`). The page renders it as a table through `core-tables.ts`, with player wording keyed by row, so a new cost row fails the build until it has wording. The two lists beside it, `failure_never` and `success_gives`/`success_never`, are plain sentences in the table and are written as prose on the page.

### What this chapter needs from the others

The Rules of Play states these rules and links out for the rest. Nothing below was edited from this page.

- **Compendium, Actions** must mark which entries a called roll can use (the entries whose `rolled` is `when_called`: Spot, Size Up, Survive, Persuade, Endure, Sneak, Recall, Ride, Fly). The page sends readers there for the list rather than naming or counting them.
- **Making Your Soldier** must keep the steps for an act outside the Catalog as the default order outside a fight, say that the list of values they name is an index and not a gate, and say that the steps are not used in a Titan Engagement or a Skirmish.
- **Fighting Titans** and **Skirmishes** must carry the worked examples of improvised acts and model entries this page points at, and Fighting Titans must state that the Circumstances of a fight are Standard unless the GM names a step.
- **Gear & ODM** must let a ruling name a fall (low or high, never extreme, no Height step raising it, inside a fight as outside one), and give the caps on Squad Supply found on a successful called roll.
- **Wounds & Fear** must carry the one Fear Roll trigger the GM reads: outside a Titan Engagement, an event at least as horrifying as a listed trigger.
- **Expeditions & Downtime** must state that a Leg Hazard's or a Night row's own roll takes the step in force when the hazard is rolled, and that no step is named for the danger the row itself describes.

## Compendium (`site/src/pages/compendium/`: Talents, Actions, Specialties, Origins & Lifepath)

Player wording for Talents and Action Catalog entries lives in `site/src/content/compendium/talent-text.yaml` and `action-text.yaml`, keyed by row id.

1. **Formation Drill and Recruit in the first campaign.** `data/character/squadmates.yaml` (`playtest_configuration`, `while_it_holds`) says no Squadmate serves in the first campaign, so Formation Drill has no effect there and the Recruit Squad Action is not offered (`data/campaign/downtime.yaml`, `squad_actions`, recruit). The configuration is a campaign setting, not the default rule (`default_rule`), so the Formation Drill card and the Squad Action entry state the default rule and say nothing about the first campaign. Owner to confirm.
2. **Rally outside a Titan Engagement.** `CONTEXT.md` (Rally) says Rally is not an action outside a Titan Engagement. `data/character/action-catalog.yaml` gives `rally` the kind `action` with context `any` and leaves the rest to Chapter 3. The Actions page follows the table (kind: Action) and sends readers to Wounds & Fear for where and how it is made.
3. **The horse on Break Attention.** `action-catalog.yaml` lists the horse as a Break Attention gear item with no "while mounted"; only the dodge and Leap Clear carry the mounted note (row comments; `docs/rules/02-character-creation.md` 555-564). `data/engagement/attention.yaml` (`break_attention`, `gear`) defers to `horses.yaml` `gear_dice`. The page adds "only while mounted" to the dodge and Leap Clear, not to Break Attention. Unclear whether a Break Attention made with the horse needs the soldier mounted (Loose the Horse reads as sending the horse away).
4. **Canon Ties name canon characters.** `data/character/origins.yaml` rows name Hannes, Armin Arlert, and Sasha Braus as Canon Ties. The voice guide bars canon characters from in-world quotes and examples; the Origins page shows Canon Ties as table rows, as the table gives them. Owner to confirm.
5. **Blade Discipline's limit.** The `limit` is `once_per_titan_engagement`, and the row comment adds "also once per Skirmish". `limit_terms.once_per_titan_engagement` already extends that limit to a Leg, a Night Camp, and a Skirmish, so the card shows "Once per Titan Engagement" and the Talents page key explains the extension for every such Talent. No disagreement; noted so a reviewer does not flag the missing Skirmish.
6. **What Fly, Treat Injury, and Rally need.** The Catalog gives no `needs` for Fly and "set by Chapter 3" for Treat Injury and Rally. The cards say the calling rule or Wounds & Fear sets it, rather than giving a number.

## Making Your Soldier (`site/src/content/rules/making-your-soldier.mdx`)

`C` = `docs/rules/02-character-creation.md`; tables are in `data/character/`.

1. **Left out: the first-campaign Squad row.** C 719-725 and `squadmates.yaml` 128-147 (`playtest_configuration`): one named campaign runs 4 player characters and no Squadmates, Recruit is not offered, and no one is promoted. It binds a single campaign and cannot be named without a banned word, so the page states only the default rules (`starting_squad`, `promotion`). Needs an owner call: drop it from the Player's Guide for good, or give it a GM's Guide or Early rules note.
2. **The values an improvised action names.** C 597-607 and `action-catalog.yaml` 64-113 (`tracked_values`) give the closed list step 1 of actions outside the Catalog names. The plan leaves Appendix 2A.5 out and bans "tracked value", so the page says "one of the values the rules name" and points to what each Compendium Actions entry changes. That page must show every entry's `changes`, and it cannot show the two values no entry changes (`position-change`, a move; `rule-named-value`), which Fig. 3 steps 2 and 5 cover.
3. **Rules not yet written, trimmed.** C 12, 51, 121, 133, 469, 508, 607, 716 name XP, Train, Rank, the Canon Clock, Chases, and Operation Frames as unwritten. The page keeps only what binds play: the Campaign Year "changes only when a rule names a change, and no current rule does"; Talent levels after creation come "only from XP and the Train Downtime Action, which these rules do not yet cover"; the Squad "aims for 6 soldiers" with no Rank caveat; a Canon Tie "has no effect on play"; the turn tests outside a Titan Engagement name Skirmishes only (C 420 also names Chases).
4. **Measured figures rephrased.** C 718 (fewer Squadmates: 0.057 against 0.022 and 0.187 against 0.058 deaths a fight) and C 789 (Health 2 Squad: 0.076 against 0.018 soldiers lost a fight) quote simulation results. The page says "about three times deadlier" and "about four times as many soldiers a fight as the same Squad at Health 4". C 789's Squad also had two helper Squadmates (`attributes.yaml` 131-148), which the page does not mention.
5. **Exam requirements: prose shorter than the table.** C 355 lists the ignored requirements with "such as" (a Titan, a Position, a patient, a comrade with a Stress Response, a worn item). `graduation-exam.yaml` 50-54 also lists Attention, a Grabbed or Down soldier, and a Jammed item, and says the exam issue item always meets the gear requirement. Page follows the table.
6. **Talent fallback that cannot fire today.** C 195-202 and `training-years.yaml` 24-26: the second fallback covers a Talent that names only dormant or reserved entries, and C 202 says none exist. The page keeps the rule as "names only entries no rule calls for", avoiding the banned words; C 193's "a player may still take it" and the "every row offers a live Talent" guarantee are left out as statements about the tables, not rules.
7. **Unclear: Break Free for a Grabbed comrade.** `enlistment.yaml` 73-74 counts "Break Free taken on a Grabbed comrade's behalf (Pry Loose)". It is unclear whether this needs the Pry Loose Talent or names it as the usual way; C 428 does not list the uses. Page keeps the source's meaning as "(with Pry Loose)".
8. **Freedom's note names a draft chapter.** `enlistment.yaml` 247 (`notes`) reads "Chapter 4 defines when a soldier is airborne". The table's wording map renders it as "Gear & ODM defines airborne."
9. **The Squadmate template table checks Health and Resolve in code.** `site/src/lib/character-tables.ts` recomputes each template's Health and Resolve from its ratings with the `attributes.yaml` 152-168 formulas and fails the build if `squadmates.yaml` disagrees. If those formulas change, the check must change with them.
10. **Design notes left out.** C 88, 293, 323, 392-394, 438-440, 510-512, 593, 619, 629 (reference Rookie and Veteran builds, simulator targets), 686, 712, 754, 791 are design notes, not rules, and are not on the page.

## Resync after the called-roll revision (2026-09-16)

Chapter 2, the character tables, and the Compendium wording were resynced to the revision that makes GM judgment valid (called rolls, Circumstances, improvised acts). Notes from that pass:

1. **The values an improvised action names are no longer a gate.** `action-catalog.yaml` (`tracked_values`) now calls the list an index kept for Talents and for the effects an improvised act can take, not a gate, and the outside-Catalog steps are "the default order" outside a fight. The page keeps the ban on "tracked value", so it says "one of the values the rules name" and then states in its own words that the values are an index of what soldiers change by acting, not a gate on what a player can try.
2. **Circumstances belongs to The Rules of Play.** The ladder, its steps, and the stakes menu are that chapter's. Making Your Soldier states only what Chapter 2 owns (no Circumstances on a Trial roll, no Talent changes a step, a called roll's Circumstances named before the pool) and links to that chapter for the rest. The glossary entries for Circumstances, called roll, passive roll, stakes, and ruling are added with that chapter, so this page uses the terms in plain text and leaves the pop-ups to the Compendium cards, which glossify by themselves.
3. **Relentless on a modelled Nape strike.** `titan-harm.yaml` (`nape_strikes`, `modelled`) spells out that Relentless adds 1 more Opening on at least 1 success. The page says the act is resolved as a Nape strike that falls short, which is the Talent's own trigger, and does not name the Talent again. No disagreement; noted so a reviewer does not flag it missing.
4. **Unclear: Help on a called roll made by a Squadmate.** The Catalog's called-roll Help rows say "up to three comrades who are present and able to help concretely may Help, as the GM rules". Nothing says whether a Squadmate is one of them, as the Requisition rule does for its own Help. The page and the cards keep the source wording ("comrades"), which reads as including Squadmates.
5. **Unclear: a called roll's retry.** `dice-pool.yaml` (`called_roll`, `retries`) covers retries, and in a fight says the act is tried again on a later turn. The Compendium cards say what a called roll needs and who can Help, and leave retries to The Rules of Play, where the retry rule lives.
6. **Called-roll examples in the entry cards are the site's own.** The Catalog gives no example act per entry. Each card's "such as ..." (a rooftop for Fly, a ruined house for Spot, a watch post for Sneak) is written for players from the entry's own meaning, not lifted from a row. Owner to confirm the tone.
7. **The Actions page gained a "Called roll" tag.** `shared-data.ts` reads `rolled` and marks the nine entries; an unknown `rolled` value now fails the build, as an unknown `kind` already did. The Compendium claims file records it.

## Fighting Titans (`site/src/content/rules/fighting-titans.mdx`)

`C` = `docs/rules/05-titan-engagement.md`, `C6` = `docs/rules/06-standard-titans.md`; tables are in `data/engagement/` and `data/harm/effect-types.yaml`, rendered by `site/src/lib/engagement-tables.ts`.

1. **"Flag" is a banned word, and three rules rest on flags.** `attention.yaml` (`flags`) names hooked-by-strike, just-hurt, and loudest. The page calls them **marks**, and names the three in the GM pages' plain words: *hooked in by a strike*, *just hurt it*, *loudest*. The noun is the site's own; neither `CONTEXT.md` nor the drafts give one. The GM's Titan pages use the same three phrases with no collective noun (their note 2 below). If the owner prefers one word for both, "mark" is the page's proposal.
2. **The Heave rating is public here and withheld in the Compendium.** `size-classes.yaml` gives every class a `heave`, and `titan-format.yaml` (`stat_block`, `standard_titan`) says a standard Titan takes it from its Size Class row, so it is public like Tempo and Nape Depth. `site/src/lib/shared-data.ts` loads `heave` only for the GM's Guide (`audience === 'gm'`), so the Compendium's Titan pages show no Heave rating. The page therefore renders it from the Size Class table and points readers at the Compendium for the rest of a Titan's numbers. Owner to decide whether the Compendium should show it too.
3. **Leaving during a retreat: two rows disagree on corpses.** `background-titans.yaml` (`retreat`, `moves`, option 2) opens leaving to a soldier who "holds distant relative to every Focus Titan". `positions.yaml` (`leaving`, `who`) requires Distant relative to every Focus Titan and adds "a corpse counts as a Focus Titan for this test". C 892 follows the first and C 916 the second. The page follows `positions.yaml`, the rule that owns leaving: Distant from every Focus Titan **and** every corpse.
4. **The setup table is left off the page.** `engagement-setup.yaml` and C 127-177 roll the Anchor Rating, the Focus Titan's Size Class, the Abnormal, the Background Titans and their clock lengths, and give the retreat clock 8 segments. All of it is the GM's, so the page says only what a player sees of the start: who takes part, that everyone begins Distant, that the Titan starts clean, the two Fear Rolls, and that Squad Tactics become unused. The retreat clock's length is therefore never given a number on the page.
5. **Rolling the Next Behavior is left off.** `behavior-procedure.yaml` (`next_behavior`) gives the moments it is rolled, the no-back-to-back rule, and the move-up past Broken parts. The page states only what players can see: the Titan always has one, it is rolled out of sight, and nothing changes it once rolled. The card procedure is on the page in full, because players resolve their own dodges against it.
6. **Steam's location roll.** `titan-harm.yaml` (`steam`) sets `injury_location: rolled` for the Critical Injury steam inflicts at 0 current Health, and C 722 says the same. The page says steam "reaches the Critical Injury tables only at 0 current Health" and leaves the location to Wounds & Fear, which owns the roll.
7. **The Pinned entry lists are rendered from the harm tables.** `data/harm/effect-types.yaml` (`pinned`) holds both closed lists as Catalog ids, and the page renders them through `action-catalog.yaml`, so a new entry that is neither allowed nor forbidden simply does not appear. The rendered block in C 738-743 is the same two lists. If a later entry is added to the Catalog without being added to either list, neither the chapter nor the page will show it.
8. **The Grab escapes table checks itself against the Grab rows.** `engagement-tables.ts` (`grabEscapesTable`) reads `needs`, `successes_to_free`, `lifted_penalty`, `reach_before_lift`, and `reach_after_lift` from `grab.yaml` and fails the build when a reach or a penalty no longer matches the player wording beside it, so a change to the Grab's reach cannot go unnoticed.
9. **Round time and the tuning sections are left off.** `round.yaml` (`round_time`) and C 973-1157 are design targets and measurements. Nothing from them is on a player page.
10. **Unclear: a horse at Distant and the riderless-horse decoy.** `attention.yaml` (`decoys`, `riderless-horse`) needs the horse at the soldier's Position "compared relative to the Focus Titan recorded with the horse's Position", and sets no Position requirement of its own, so a soldier at Distant may send a horse, while Draw Attention may not be taken from Distant. The page states the requirement as the table gives it and adds no Position limit.
11. **The round drawing is fixed at five steps.** `RoundDiagram.astro` lays `round.yaml` (`round_steps`) out across the five-station drawing in `rules.css` and throws if the table ever holds a different number, so a new round step fails the build rather than crowding the drawing.
12. **`StepFlow` no longer demands exactly five steps.** The card procedure is eight stations. The component runs as rows at every width, so the guard now only refuses a drawing of fewer than two. The Rules of Play's three five-step drawings are unchanged.

### What this chapter needs from the others

- **Wounds & Fear** must carry: the Critical Injury tables with their locations, types, worsening, and the Net Success rider; the Fear Roll triggers for a first Titan Engagement, an Abnormal, a second Focus Titan, a comrade Grabbed, and a comrade dying; the forced step and the loudest mark a Fear Roll result can set; Down; Treat Injury and Rally; and every step that follows the end of a Titan Engagement, including the Stress relief for a Nape kill and for the ending, Grief, and the care window.
- **Gear & ODM** must carry: working ODM Gear and the Jam, the Gas Roll and what counts as ODM use, airborne, the fall bands with the Height step at Giant Forest and the Large Titan's raise, mounting and dismounting, Horse Whistle and a lame horse, carrying and Lift Comrade, Field Repair, the cloak as an object that is not a gear item, and Squad Supply flares.
- **The Rules of Play** already carries the Circumstances ladder, the called-roll procedure and its closed stake menu, the attack and Reaction procedure, Help, Covering, and what a turn spent in advance means. This page links there rather than restating any of it.
- **The Compendium** must show each Titan's stat block and Behavior Table, and the Action Catalog entries this page names by rule rather than by requirement: Nape strike, Body Part strike, Break Attention, Draw Attention, Read, Call It, Break Free, Heave, Leap Clear, and the dodge.

## Owner decisions

- **First campaign setup (2026-09-16).** The rule that a first campaign runs four player characters and no Squadmates is left off the site. It is a playtest setup, not how the finished game is played.
- **Rewrites paused (2026-09-16).** Chapter rewrites wait until the current rules revision is committed to main. Finished pages then get a resync pass.

## The GM's Guide Titan pages (`site/src/content/gm/running-a-titan-fight.mdx`, `titans.mdx`, `tuning-the-fight.mdx`)

1. **The specimen report drops four Behavior Table columns.** `site/src/components/TitanSpecimen.astro` renders a Behavior Table of five columns (d6, behavior, tier, Attack Dice, what happens). Every Behavior Table in `docs/rules/06-standard-titans.md` also carries Targets, Holder's Position, Body Parts used, Fallback, and the Grab mark, and a GM cannot resolve a card without them: the fallback and the move-up both read those cells. The GM's Titans page renders the missing columns beside each dossier, from the Titan's own table through `site/src/lib/gm-tables.ts` (`behaviorRequirements`). The Compendium's squad-facing Titans page and `/gm/titan-dossiers/` still show the short table. Owner to decide whether the Compendium should carry them too, or whether the specimen report should grow the columns and the GM table then goes away.
2. **"Flag" is a banned word, and the Attention rules are built on flags.** `data/engagement/round.yaml` (`gm_tracker`, `focus_titan_row`) tracks "flags this Titan has set, by soldier (hooked-by-strike, just-hurt, loudest)", and `data/titans/index.yaml` (`ladders`) names the rungs that read them. The GM tracker row is worded "What it has noticed", and the three are named in plain words ("hooked into it by a strike", "having just hurt it", "loudest"). Fighting Titans must use the same plain words for the same three things, or the two pages will read as different rules.
3. **The lone-strike and Jam headlines are left off Tuning the Fight.** `docs/rules/05-titan-engagement.md` 1034 (a lone soldier's Nape strike after Break Attention) and 1097 (the Jam test) are headline expectations that cannot be stated without a percentage, which the page's brief excludes. The page carries the Grab's "about 1 in 3, about 2 in 3" because `ADR-0014` states it in words. The Large Titan's "no kill in at most 15% of fights" is on the page as "a real share of fights", and the Large Titan's template row as "far more fights with no kill".
4. **The Abnormal share is computed, not copied.** `docs/rules/05-titan-engagement.md` 155 and `data/titans/index.yaml` both state "1 Titan Engagement in 12". The page derives it from the Medium share of the Size Class roll and the Abnormal share of the second roll, so a change to either row moves the sentence instead of leaving it stale.
5. **Tracker wording is keyed by the opening words of each entry.** `gm-tables.ts` keys the GM tracker's wording map by the line plus the first five words of the table's own text for that entry, so a reworded or added entry loses its wording and fails the build. That is deliberate, and it means a small rewording in `round.yaml` needs a matching key here even when the meaning has not changed.
6. **Round time is a target, not a measurement.** `data/engagement/round.yaml` (`round_time`) states the band as a design target and names the test that will measure it. The page gives the band as plain guidance ("12 to 20 minutes", "at most 30 for two Focus Titans") and says nothing about the test.
7. **The lethality guidance will move.** `data/titans/tuning.yaml` records that the deaths band was re-set twice and is to be re-set again. Tuning the Fight is written in words that survive a re-set ("no single standard fight is expected to kill"), but it should be re-read when that band changes.
8. **What the GM pages need from Fighting Titans.** Nothing on a player page links here. The GM pages link out to `/guide/fighting-titans/` for the card's resolution and the dodge, the Read facts and their prices, Attention and the standard ladder, the Grab, and the two tests that end a fight; and to `/guide/rules-of-play/` for the Circumstances ladder, the called-roll procedure, the closed stake menu, and the full list of what a ruling never touches. Those sections must exist on those two pages.

## Wounds & Fear (`site/src/content/rules/wounds-and-fear.mdx`)

`C` = `docs/rules/03-harm-and-mind.md`; tables are in `data/harm/` and `data/mind/`, rendered by `site/src/lib/harm-tables.ts`.

1. **"Flag" is banned, and one Fear Roll effect is named after one.** `effect-types.yaml` (`draw-attention`) is named "The loudest flag". The page calls it **the loudest mark**, the noun Fighting Titans and the GM's Titan pages already use for the same three things. Every effect name on the page comes from the wording map in `harm-tables.ts` rather than from the table's own `name`, for this reason.
2. **The type riders are rendered as one table, not as a line under each of the four.** `critical-injuries.yaml` (`type_riders`) hangs riders off each Injury Location table, and the chapter prints a paragraph under each. The page renders them as a single grouped table, one group per Injury Location, one row per rider, so a new rider on any table appears without new page text. A rider that sets a field the wording map does not know fails the build.
3. **Unclear: the Injury Location roll takes no modifier, and the page does not say so.** `critical-injuries.yaml` (`injury_location_table`, `modifiers: none`) states it as a table fact; C 143 does not repeat it. The page says the roll is made unless the harm's rule names a location and adds nothing, so a reader could imagine a bonus. Left as the source has it.
4. **A day's steps are keyed by their opening words.** `healing.yaml` (`each_day`) is a list of sentences with no ids, so `harm-tables.ts` keys their player wording by the first words of each line. A reworded step loses its wording and fails the build, which is deliberate, but a small rewording needs a matching key here.
5. **The sheet-field list is left off.** C 910-926 (`sheet-fields.yaml`) specifies what this chapter adds to the character sheet and the Squadmate stat block. It is a sheet specification, not a rule a player reads in a chapter, so the page carries none of it; the two rules inside it that bind play, how current Health is derived and that the recorded Down state must match the two conditions, are on the page in their own sections. The character sheet, when it is drawn, must carry the rest.
6. **Prose shorter than the table: what the both-arms grade forbids.** C 301's rendered row and `critical-injuries.yaml` (`forbids_entries`, `forbids_decoys`) list the entries by id. The page renders them from the Action Catalog, so the list is exactly the table's and grows with it. The same holds for the Pinned lists, which Fighting Titans renders.
7. **Squad Supply, the medical kit, and Field Repair are named but not priced here.** C 504 gives them to Chapter 4. The page states only what the Burn rider needs (a kit, or 1 medical unit spent on the roll, which is the care bonus) and links to Gear & ODM for the item, restocking, the Bonus Die, and which care windows take a Field Repair.
8. **`Cell` gained a `lines` variant.** `core-tables.ts` and `CoreTable.astro` now render a cell as a list of lines, for the five names of an Injury Location row and for a grade's moves. Existing tables are unchanged.
9. **`StepFlow` now takes an icon from the site's icon set.** It read only line icons; a name with no colon is now drawn as an icon slot, as `RoundDiagram` already did, so the injury walkthrough can use the harm and injury icons.

### What this chapter needs from the others

- **Gear & ODM** must carry: the medical kit and what "counts as had" means, restocking it, the medical Squad Supply's care bonus, Field Repair in the care windows, falls (low, high, and extreme bands, and that a fall is Crush), what happens to a soldier who becomes Down while airborne or mounted, Lift Comrade and carrying, the Blade Set swap from a carried set, the Gas Roll, prosthetics as gear items and their Requisition, and what happens to a dead soldier's gear and to a dropped Blade Set when a fight ends.
- **Fighting Titans** already carries the Grab and its countdown, the falling body, Pinned, steam, corpse heat, witnesses, and a Down soldier's move. This page links there and states only the harm side of each.
- **Expeditions & Downtime** must carry: when a day passes at a Night Camp, the Camp Relief, Downtime's infirmary with its seven days and its one care window, Visit Haven, Honoring the Fallen, the Squadmate relief, Recover, and fitting a prosthetic during a Downtime.
- **Skirmishes** must carry: Treat Injury as an action with no Position requirement and who may Help it, the first-kill Fear Roll trigger, the end steps read with Skirmish in place of Titan Engagement, and a Foe's weapon as damage with its Injury Type.

## Gear & ODM (`site/src/content/rules/gear-and-odm.mdx`, `site/src/pages/compendium/gear.astro`)

`C` = `docs/rules/04-gear.md`; tables are in `data/gear/`, rendered by `site/src/lib/gear-tables.ts` with player wording in `site/src/content/compendium/gear-text.yaml`.

1. **Block and Fight are live, and the chapter calls them reserved.** C 232 says the Blade Set in the handles supplies Gear Dice "for the Nape strike, Body Part strike, Break Free, and the reserved Block and Fight". `data/character/action-catalog.yaml` marks neither `block` nor `fight` dormant or reserved: both are Skirmish entries with requirements, needs, and Help rules. `data/gear/items.yaml` (`blade-set`, `gear_dice_for`) lists all five with no mark. Page follows the tables and names all five, with no word for how new they are. The build also checks that each item's `gear_dice_for` matches the Catalog's own `gear` lists; today all ten agree.
2. **The Blade Set rating is a column of its own in the source table and a note on the page.** `standard-issue.yaml` keeps `every_row.blade_set_rating` outside `by_funding` because it is the same at every Funding, and the rendered block in C 537 gives it a column that repeats 1 six times. The page folds it into the Blade Sets cell ("3, each rated 1"). If a later Funding row ever rates Blade Sets differently, the value moves into `by_funding` and this cell must change with it.
3. **The interim issue replaces more of a horse than the prose lists.** C 71 (*Restoring*) says the interim issue "replaces a lame or worn horse". `standard-issue.yaml` (`interim_issue.differences`) gives the horse step as the full issue's, which also replaces a horse the soldier lacks and one rated below the Funding row. Page follows the table.
4. **A Gas Roll a Fear Roll result calls for has no row in the gear tables.** C 183 states it, and `data/gear/odm-gear.yaml` does not: the row lives in `data/harm/effect-types.yaml` (`gas-roll`), which Wounds & Fear owns. The page states the rule from the chapter, under the Gas Roll, and leaves the trigger to that page. If the harm table changes the dice, this page must change with it.
5. **The Squad Supply kinds carry no player text of their own.** `squad-supply.yaml` (`kinds.medical.uses`) reads "medical_uses, below", a pointer rather than a sentence, and the other three kinds cite draft chapters. All four kinds are written for players in `gear-text.yaml`, keyed by the kind's id, so a fifth kind fails the build until it has wording.
6. **The carrying table is keyed by each row's own words.** `carrying.yaml` (`items_counted`) rows have no ids, so `gear-tables.ts` keys the player wording by a slug of the row's `what`. Rewording a row loses its wording and fails the build. That is deliberate, and it means a small rewording there needs a matching key here even when the meaning has not changed.
7. **Two icons are shared and one is a placeholder.** The site's gear set has no tool kit icon, so that card carries the empty frame under the name `gear-tool-kit`. The flintlock pistol, the musket, and shot share `gear-firearm`; the two prosthetics share `gear-prosthetic`; medical supplies share the medical kit's icon. Art can fill the gap without any change to the page.
8. **Unclear: what "the interim day" is, from this page.** C 522 and `standard-issue.yaml` (`interim_issue.when`) both hang the interim issue on the interim day and its care window, which `data/harm/healing.yaml` owns. The page names it without defining it and expects Wounds & Fear to carry the day and the window.

### What this chapter carries for the others

- **The Rules of Play** asked for two things, and both are on the page. A ruling may name a fall, low or high as the GM names with the stakes, never extreme, with no Height step raising it, inside a Titan Engagement as well as outside one, and low if none is named (*Falling*, *When you fall*). The caps on Squad Supply found on a successful called search, at most 1 unit per success of one kind, one search a place, at most two searches a Waypoint scene, and none during a fight, are in the ruling box at the end of *Standard Issue and Squad Supply*.
- **Fighting Titans** asked for working ODM Gear and the Jam, the Gas Roll and what counts as ODM use, airborne, the fall bands with the Giant Forest and Large Titan raise, mounting and dismounting, Horse Whistle and a lame horse, carrying and Lift Comrade, Field Repair, the cloak as an object that is not a gear item, and Squad Supply flares. All are on the page.

### What this chapter needs from the others

- **Wounds & Fear** must carry: damage and the Health boxes a fall marks; Down; Critical Injuries with their locations and types, and the Crush type a fall gives; care windows, their scope, and the interim day; Treat Injury, which medical supplies buy a Bonus Die on; the lost-limb grades, including the both-legs grade Lift Comrade reads and the both-arms grade mounting reads; and the Fear Roll results that drop the Blade Set in the handles and call for a Gas Roll.
- **Fighting Titans** must carry: Positions and which Position changes need an ODM move; where a fall lands; letting go; the Grab, whose hold ends the airborne and mounted states before its crush; the grounded Titan, the one rule that lifts the working-harness requirement on strikes; Break Attention and its decoys, the riderless horse among them; and Pinned, which forbids a lift.
- **Expeditions & Downtime** must carry: Funding and the Requisition that is the only source of firearms, prosthetics, and kept items; Scarcity; the Maintain Gear Downtime Action, the only rule that restores every rated item; what a Leg and a Night Camp spend in rations and what a Squad with none pays; the depot Waypoint's issue and the issue before each Expedition; and the Waypoint scene the search caps count.
- **Skirmishes** must carry: Shoot and Reload, a firearm's damage, its Injury Type, and which Foes it can be fired at; the flare fired at a person; and the move a musket's Reload spends there.

## Expeditions & Downtime (`site/src/content/rules/expeditions-and-downtime.mdx`)

`C` = `docs/rules/07-playtest-rules.md`; tables are in `data/expedition/` and `data/campaign/`, rendered by `site/src/lib/expedition-tables.ts`.

1. **The hazard tables themselves are left off, and the page does not link to them.** The Leg Hazard table's rows, the Night table's rows, the interim route and its two D6 tables, and the night example are the GM's. No player page links into the GM's Guide, so the page names neither a row nor a total: it states what both rolls count (a rendered table of the modifiers, which the Squad's own Post, Pace, Leg roll, and flare move), that both are rolled in the open, and that what comes up is read from the Commander's copy and never softened or swapped. If the owner would rather players saw the hazard rows, the modifiers table is already the place to hang them.
2. **The Straggler's fallback names a hazard row.** C 216 and `hazards.yaml` (`straggler.victim.pool`) say that with no soldier of either kind the row "reads as titan-on-the-formation", which is a row on the table the page does not show. The page says "the hazard is resolved as a plain Titan attack on the formation instead", which is that row's own effect: a Titan Engagement with every soldier at Distant.
3. **The Rules of Play's request is carried.** That page asked this one to state that a hazard row's own roll takes the step in force when the hazard is rolled, and that no step is named for the danger the row describes (C 273, 276; `hazards.yaml` `rolls.straggler-endure.notes`). Both are in *The hazard*, with the fight a row begins taking its Circumstances as any fight does.
4. **The interim day is named and not defined here.** C 96 and `legs.yaml` (`expedition.interim_rows`) say the interim day and the interim issue never happen during an Expedition or a Downtime, and the day itself belongs to `data/harm/healing.yaml`. The page states the rule and leaves the day to Wounds & Fear, as Gear & ODM's note 8 already expects.
5. **The first-campaign configuration is left off, as the owner decided.** `downtime.yaml` (`steps.squad-action`, `squad_actions.recruit`) says Recruit is not offered and brings no Squadmate while the playtest configuration holds, so the Squad Action would always be Honoring the Fallen. Following the owner decision of 2026-09-16, the page states only the default rules: both Squad Actions are on the table, and Recruit is written as its own rule.
6. **What waits for a later phase is compressed.** C 339 lists Train, Investigate, Contribute to Research, lobbying for Funding, HQ Upgrades, XP, Commendations, and Faction Standing as unwritten. Naming the phase is design vocabulary, so the page says once that training, investigation, research, and the other ways to spend a Downtime are not yet written into these rules, in the wording Making Your Soldier already uses.
7. **The interim route is the GM's, so the Hard Ride's "when" is rewritten.** `legs.yaml` (`pace.hard-ride.when`) gives two triggers, the Brief's listed day and the interim route's D6 after the day's second Leg. The page's Pace table says only that Command orders one, which covers both without showing the roll the GM makes.
8. **Two cells are computed rather than copied.** The hazard table's Formation Post line reads "+1 to -1, as the Post gives", derived from the Posts' own modifiers, and each Scarcity's "Funding that reaches it" is derived from the Funding gate rows and marked open or closed at the current Funding. A change to either table moves the cell instead of leaving it stale.
9. **The Squad Supply a called search finds is capped in two places, and they agree.** `legs.yaml` (`day.waypoint_scene.runs_on`) caps a Waypoint scene at two searches, and `data/gear/squad-supply.yaml` (`spending`) holds the per-search caps that Gear & ODM already states. The page gives the two-search cap here and links to Gear & ODM for the rest.
10. **An infirmary day's steps are keyed by their opening words.** `downtime.yaml` (`infirmary.each_day.steps`) is a list of sentences with no ids, so `expedition-tables.ts` keys their player wording by the first five words of each, as `harm-tables.ts` does for a day passing. A reworded step loses its wording and fails the build, which is deliberate, but a small rewording needs a matching key here.
11. **Downtime's day count is checked in code.** `downtime.yaml` states seven days in `downtime.days` and again in `infirmary.each_day.count`; the page reads both and fails the build if they ever disagree.

### What this chapter needs from the others

- **The GM's Guide** must carry the Leg Hazard table, the Night table, the interim route, the Waypoint kind and Formation Post D6 tables, the Mission Brief the GM writes as Command, and the night example. The player page states the Brief's bounds, not how to roll one.
- **Wounds & Fear** must carry the interim day and its care window, the care window held after harm outside a fight, Death Rolls and day limits, healing time and the riders that stop it, Grief and the Grief of a day passing, prosthetics and when they are fitted, and medical Retirement.
- **Gear & ODM** already carries Standard Issue and the full issue, Squad Supply and its stock, the caps on a called search, kept items, firearms, prosthetics as items, Field Repair, horses and lameness, and falls. This page links there rather than restating any of it.
- **Making Your Soldier** must carry the roll-off for shared choices, promotion and Retirement and their timing, and the Squadmates the Squad Actions and the Squadmate relief read.
- **Fighting Titans** must carry starting a Titan Engagement, the Anchor Rating a Waypoint kind sets, the retreat, and the starting Positions a hazard row names.

## Skirmishes (`site/src/content/rules/skirmishes.mdx`, `site/src/pages/compendium/squad-tactics.astro`, `site/src/pages/compendium/scars.astro`)

`C` = `docs/rules/07-playtest-rules.md` (section 7.4); the table is `data/skirmish/skirmish.yaml`, rendered by `site/src/lib/skirmish-tables.ts`.

1. **A holder's move: "cannot move" against "the move changes nothing".** `skirmish.yaml` says both, in two places: `engaged_and_apart.held` gives that the move of a soldier who holds a Foe changes nothing, and `held.the_holder` gives that the holder cannot move. C 24 and C 116 carry the same two wordings. The page follows the movement rule, so the holder still takes a move and it changes nothing. The two readings part only over a move that would carry a mount or a dismount with it, which the page therefore leaves shut: nothing about the holder's range changes while the hold lasts.
2. **Which entries an improvised act cannot model.** C 61 says "never Block, Dodge, or an option such as Reload or Release". `skirmish.yaml` (`actions.improvised_act`) is longer: never a Reaction, a roll, a fixed roll, or an option, "which rules out reload, release, swap-blade-set, shed-load, and mount-or-dismount". The two agree, because the model entry must already be of kind action, which excludes every roll. The page keeps the chapter's shorter wording and says "an option such as Reload or Release", which covers all five.
3. **Unclear: a Skirmish has no Positions, but its end steps read one.** C 22 says a Skirmish has no Position at all, and C 218 then resolves the aftermath rolls "with every soldier who took part counting as holding the patient's Position". The page states the substitution once, in *And then the same steps as any fight*: everyone who took part is who those steps mean by everyone who held a Position. Nothing on the page gives a soldier a Position inside a Skirmish.
4. **Grit is named on the player page, and its numbers are not.** The per-kind Grit, Parley value, Watch, Health, Attack Dice, Guard, and group size stay in the GM's Guide with the Foe stat blocks (`data/skirmish/foes.yaml`). Three of those names still have to reach a player: Size Up states the group's Grit aloud (C 202), a Parley reads the group's Parley value (C 185), and the Sneak for the Ambush needs its Watch (C 240). The page names all three as things the table tells you, prints no value, and links nowhere, since no player page links into the GM's Guide.
5. **Foe weapons are on the player page.** The weapons table (`skirmish.yaml`, `weapons.rows`) holds the sabre, knife, club, and firebrand alongside the soldiers' own, each with its damage and Injury Type. They are printed, because a player needs to know what a Foe's blow deals and because the same rows say what those objects do in a soldier's hands. The rows carry no Foe's numbers.
6. **The end-of-fight steps are linked, not repeated.** C 212 to 223 lists the nine steps in full for a Skirmish. `data/harm/engagement-end.yaml` is one table for both, and Wounds & Fear already renders it, so the page states only the two readings that change and points there, as Fighting Titans does.
7. **Worked examples show a Foe's public rolls.** The two examples print a Guard roll of three dice and an attack of four. Both are rolled in the open at the table, so they are what a player sees, but they do come from a Foe's row. If the owner would rather no Foe number appeared on a player page at all, the two `PoolRows` in *An exchange in the doorway* are the only place to change.
8. **Squad Tactics and Scars carry no new rules.** Both Compendium pages render their tables through wording keyed by row id, and every sentence on them already stands on a chapter page. `data/engagement/squad-tactics.yaml` (`rules.held`) is the one place the number a Squad holds is written, so the page reads it out of that sentence and fails the build if it stops saying it.

### What this chapter needs from the others

- **The GM's Guide** must carry the Foe stat blocks, the closed rule every Foe acts by and the GM's direction of one with a reason said aloud, the per-kind Grit numbers and breaking a group sooner, the ruling on whether the Squad can approach unseen, and the Foe firearms rule.
- **Expeditions & Downtime** must carry the Night table's prowlers row as the thing that starts a Skirmish at a Night Camp, and that a Skirmish begun at one is at night.
- **Wounds & Fear** must carry the nine end steps, the Cut and Pierce riders, the first-human-kill Fear Roll, and the Fear Roll the GM may call outside a Titan fight.
- **The Rules of Play** already carries the attack and Reaction procedure, Circumstances, Help, Covering, and called rolls. This page states only what a Skirmish adds.

## The GM's Guide Expedition and Foe pages (`site/src/content/gm/running-expeditions.mdx`, `foes-and-skirmishes.mdx`)

`C7` = `docs/rules/07-playtest-rules.md`; tables are in `data/expedition/` and `data/skirmish/`, rendered by `site/src/lib/gm-expedition-tables.ts` and `site/src/lib/gm-foe-tables.ts`.

1. **What sits under a bar, and what does not.** ADR-0023's bars cover values a squad should not read over the GM's shoulder. On the Foe records every number is barred (Attack Dice, Guard, Health, Grit, Parley value, Watch), and so are the two cells of the computed Parley table, because a Size Up is how a Squad buys Grit and the rest is learned by fighting. On the hazard tables the bar covers the *What happens* cell of a row whose own effects put a Titan, a Foe, or harm in front of the Squad (a Titan Engagement, a Skirmish, the Straggler, a fall, or a choice between them). The quiet rows stay open so the tables can still be read at a glance. The rule is applied in code from each row's effect types, not row by row, so a new row is covered or open by what it does.
2. **A row's own text is never printed.** Several rows carry chapter and section numbers in their `text` (the Thrown row cites the fall rules). Every hazard row, Night row, Waypoint kind, interim step, Straggler row, and Foe kind therefore has GM wording keyed by its id in the two lib files, and the numbers inside each sentence (wear, units, Stress, ration and flare spends, starting Positions, Background clocks, the Bandits' count, the Injury Type) are read back out of the row's own effects. A new row, or a row whose effects change shape, fails the build.
3. **The hazard modifiers are rendered on both the player page and this one.** `Expeditions & Downtime` renders the modifiers so players can see what their Post and their Leg roll are worth; the GM page renders them again because the GM is the one adding them up. Both come from the same rows, so they cannot drift. It is the one deliberate overlap between the two pages.
4. **The reported Skirmish figures are given in words.** `data/skirmish/foes.yaml` (`reported_figures`) holds win rates and deaths per Skirmish as percentages. As on *Tuning the Fight*, the page states them without numbers: the Squad beats a patrol nearly every time, a lone Rookie is a little better than even against one trooper, and the Bandits' night Ambush costs blood and almost never a life.
5. **The Military Police trooper's Parley value may still move.** `foes.yaml` (row comment at `parley`) records that the owner may set it to 3 or higher if the Military Police are meant to stand firm. The page's Parley table is computed from the row and the asks' own numbers, so a change there moves the table rather than leaving it stale.
6. **Bandits carry firebrands at every night Skirmish, not only the Prowlers row.** C7 590 reads "at night, a firebrand in place of a club" inside the Prowlers context; `foes.yaml` (`fight_weapon`, `at_night`) says the swap covers the Prowlers row and any Bandit group at night. Page follows the table, and says that a Skirmish begun at a Night Camp is at night.
7. **The night example is split across the two GM pages.** C7 772-789 runs from the camp roll to the day passing, through a Skirmish. *Running Expeditions* carries the camp roll, the rations, and the night hazard that gives Prowlers; *Foes and Skirmishes* carries round 1, the kill, and the break. Neither page repeats the other's half.
8. **Left off the GM pages as player-side.** `legs.yaml` (`expedition`) on who takes part, who is mounted, the interim day, gas and gear not coming back, promotion and Retirement timing, and Talent uses per Leg and per Night Camp are all on `Expeditions & Downtime`. The GM pages state only the parts a GM acts on: the no-roller rule, the Lead a hazard row can name, and the Waypoint kind that sets an Anchor Rating.
9. **No new glossary entries were needed.** Leg, Night Camp, Waypoint, Waypoint scene, Mission Brief, Distance Band, Formation Post, Hard Ride, Lead, Depot, Camp Relief, Hunger, Grit, Guard, Watch, Parley value, Ambush, Engaged, Apart, and Out of the Skirmish were already written for the player pages. The GM pages carry no pop-ups, as the other GM pages do not, so nothing was added to `glossary.ts`.
10. **`GmTable` grew two cell kinds and six tables.** It now renders a cell as a list of lines, and a cell under a bar (`{ redact, label }`), and it accepts the Expedition and Foe table ids. The types are declared in `gm-expedition-tables.ts` rather than in `core-tables.ts`, so `CoreTable` and every player table are untouched.
11. **`FoeDossier.astro` is new.** It renders one Foe kind the way `TitanDossier` renders a Titan: the record, its weapons with each one's damage and Injury Type from the weapons table, and the numbers under bars. The Compendium has no Foe page, so the record exists only on the Commander's copy.

### What these pages carry for the others

- **Expeditions & Downtime** asked the GM's Guide for the Leg Hazard table, the Night table, the interim route, the Waypoint kind and Formation Post D6 tables, the Mission Brief the GM writes as Command, and the night example. All are on *Running Expeditions*.
- **Skirmishes** asked the GM's Guide for the Foe stat blocks, the closed rule every Foe acts by, directing a Foe with a reason said aloud, the per-kind Grit numbers and breaking sooner, the ruling on approaching unseen, and the Foe firearms rule. All are on *Foes and Skirmishes*.

### What these pages need from the others

- **Expeditions & Downtime** must keep the Leg's steps, the Leg roll, the Formation Posts, rations and hunger, and the Night Camp's steps, which the GM pages link to rather than restate.
- **Skirmishes** must keep the three tests that end a Skirmish, Engaged and Apart, the attack and Reaction procedure, damage, the Grapple, and the Parley asks, which *Foes and Skirmishes* links to rather than restate.
- **Fighting Titans** must keep starting a Titan Engagement, the Anchor Rating, the retreat, and the starting Positions a hazard row names.
