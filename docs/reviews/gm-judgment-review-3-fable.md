# GM judgment: review 3 (wof-reviewer, Fable)

Date: 2026-09-16. Task: `docs/research/gm-judgment/review-task-round-3.md`, a narrow confirmation pass. Scope: the 15 round 2 findings (`docs/reviews/gm-judgment-review-2.md`, `-2-astra.md`), the round 2 fix plan, decision batch 9 items 9-36 to 9-41, ADR-0024 as amended, ADR-0009 and ADR-0014 as amended, ADR-0016 and ADR-0019, OQ-179 and OQ-180, the glossary, Chapters 1 to 7 and their YAML where those items reach, and `docs/rules/PROGRESS.md`. Not read: the packet, `site/`, the parallel Opus review. Not run: `tools/sim/run.py`. No git state changed. Throwaway dice scripts ran in the session scratchpad (300,000 trials each); figures are quoted where odds matter.

## Counts

| Critical | Major | Minor |
|---|---|---|
| 0 | 0 | 8 |

Every round 2 finding is closed by the rule as it now reads, and each of 9-36 to 9-41 is applied as its *Chapter impact* bullets are written, not approximately. The eight Minors are residues and small gaps the fixes left beside them: none lets the GM touch a result after the roll, a table row, a card, a need, or a Titan's or Foe's dice.

## Checks run, and what passed

- `tools/render/render.py check`: every rendered block matches the YAML, 55 blocks in 6 chapters. The YAML load: 67 files. `tools/probes/chapter-06/render.py check` and `run6.py check`: pass (Injury Type, Titan Dice, and Heave problems all empty; horse-state selftest passed). `tools.sim.rules.Rules()` constructs and every `guard()` in its constructor passes.
- Greps. `ADR-0003` outside `docs/adr/`, the decisions, the register, and `docs/research/`: none as a citation; the only hits are two hygiene sentences in `PROGRESS.md` lines 89 and 90 that say a file "carries no ... ADR-0003 citation", which is a mention, not a citation. `PROVISIONAL:` from batch 9: none (the `PROVISIONAL (OQ-116)` markers in `tools/sim/` and `tools/probes/chapter-05/` predate batch 9 and are the simulator's own). Em dashes in Chapters 1 to 7, `data/`, `CONTEXT.md`, the ADRs, `tools/render`, `tools/sim`, and `PROGRESS.md`: none. "difficulty": only `CONTEXT.md` lines 181, 307, and 350 (the three `_Avoid_` lines) and ADR-0024's two *Considered Options* bullets, which name the rejected mechanic.
- ADR-0016 charter item 6 reads "ADR-0024 limits 9 and 12" (line 12); ADR-0019's attack paragraph closes on "ADR-0024, limit 8" (line 5); neither file contains `ADR-0003`.
- OQ-179 and OQ-180 are Decided (`OPEN-QUESTIONS.md` lines 3675 and 3687); OQ-141 (line 3121) and OQ-167 (line 3535) carry `Revised by batch 9 (9-37)`. ADR-0009 line 17 and ADR-0014 line 55 carry the 9-37 amendments; ADR-0024 limits 7, 9, and 17 and its second `## Amended` paragraph carry 9-36.
- Dice. **9-38:** by simulation the high band (D6+2) deals 1, 2, or 3 damage at one third each and never 4; extreme (D6+4) deals 4 damage 33.3% of the time, which is the harm the step 1 fix keeps out of a ruling's reach. **9-39:** the review's build (Strength 4, Clean Cut 1, 2 Bonus Dice, 1 Gear Die, 1 Stress Die, Pushing when short of Nape Depth 4) rolls 0 successes 6.4%, 1 to 3 successes 65.2%, and 4 or more 28.3%; under 9-39 every band pays 1 Opening per success (mean 2.63 before Relentless), so the act is monotone across its range. **9-37:** at a Deep Leg with two camps made and a Rookie Lead's 7-dice Leg roll, the interim route's typical Leg (Post 0, Steady) begins a Titan Engagement 63.7% of the time, reaches Straggler or worse 47.1%, and Overrun 15.8%; the worst Leg the bounds still allow (Deep, Vanguard, Hard Ride) gives 92.6%, 80.6%, and 47.2%, the same per-Leg figure the review's all-Deep Brief gave, since the bounds cap the band by place and not the Post or the Pace, as 9-37 says. What the bounds do cap is how many such Legs there are and how many camps precede them: the worst in-bounds six-Leg Brief (Near, Far, Deep, Deep, Far, Near, Vanguard throughout, a Hard Ride on each day's third Leg) begins a Titan Engagement on 16.0, 31.4, 63.7, 64.0, 46.9, and 47.1% of its six Legs, against 4.6, 15.7, 30.8, 47.2, 30.8, and 15.6% for the same bands at Post 0 and Steady; the review's day-5, four-camp, all-Deep case (99.7% by this script) cannot be authored. The bound holds the authored worst case at the interim envelope's own Deep-Leg figure, which is what 9-37 set out to do.

## The 15 round 2 findings

| Review | Id | Sev | This round | Evidence (file and line) |
|---|---|---|---|---|
| Opus | C1 | Critical | **Resolved** | 9-38 applied verbatim. `04-gear.md:330` is the new *Height* step 1 ("A band already named ... No later step changes a band this step gives ... knock loose ... letting go ... release from a lift ... takes the band the steps below give"); steps 2 to 5 at 331 to 334 keep their words; the closing sentence at 336 reads the new "Outside a Titan Engagement there is no reference Titan and no Position to read"; the bullet at 322 adds "in a Titan Engagement as well as outside one, and the Height steps never raise it (*Height*, step 1)". `falls.yaml:57-65` (new first item of `height.steps`), `:75-77` (`outside_titan_engagement`), `:40-43` (`rule-named.height`), `:122-125` (`after_the_fall.gm`). The three band-less falls still route to the steps: `titan-format.yaml:157-163` (`knock-loose`), `positions.yaml:138-143` (`letting_go`), `grab.yaml:190-192` (release, lifted), each reading "the band Chapter 4's height steps give". |
| Opus | M1 | Major | **Resolved** | 9-36 applied verbatim in every named place. `01-core-rules.md:28` (item 1: "A called roll can be made anywhere ... it is a called roll only when no entry of kind action ... would change nothing the fight tracks ... spends the soldier's action, and Help on it is the fight's Help"), `:41` (time or position in a fight), `:45` (success in a fight), `:47` (item 4, "a called roll included, spending that turn's action and nothing else"), `:51` (Help in a fight as the fight's Help rule gives), `:72` (1.3 step 1), `:290` (1.8, the Skirmish and Titan Engagement Help sentences), `:301` and `:302` (1.9). `02-character-creation.md:619`, `:621`, `:634`; the *Acts* Drive sentence at `:422` unchanged, as decided. `05-titan-engagement.md:68` (preface bullet) and `:450` (*A called roll in a Titan Engagement*). `07-playtest-rules.md:35` (Rulings), `:524` (*A called roll*), `:709` (the rendered `called-roll` row), `:754` (the Catalog table row). `dice-pool.yaml:137-145` (`in_a_fight`), `:172-175` (`help`), `:198-199` (`fall`), `:212-214` (`time-or-position`), `:226-227` (`success_gives`), `:230-232` (`retries`), `:246` (`decided` gains OQ-179). `circumstances.yaml:156-157` (`reaches`). `action-catalog.yaml:1067-1072` (`in_a_fight`), `:1112-1116` (`gm_rulings`), `:11` (`decided`). `skirmish.yaml:143-149` (`actions.called_roll`), `:532-546` (the `called-roll` row), `:14` (`decided`). `CONTEXT.md:176` (**Called roll**, the fight sentence). ADR-0024 limits 7, 9, 17 and the 9-36 `## Amended` paragraph. |
| Opus | M2 | Major | **Resolved** | 9-39 applied verbatim. `05-titan-engagement.md:448` ("resolved as a Nape strike that falls short, whatever its successes ... Successes that reach the Titan's Nape Depth create their Openings and nothing else, and the act lowers no Nape Depth"); `02-character-creation.md:628`; `titan-harm.yaml:104-110` (`nape_strikes.modelled`); `action-catalog.yaml:1088-1092`. The threshold band is no longer empty; see Minors m4 and m5 for two residues beside it. |
| Opus | M3 | Major | **Resolved** | 9-37 applied verbatim. `01-core-rules.md:49` (item 6: "their rolls, and their modifiers, whose amounts are closed and which the route the Mission Brief sets, the night camps made, the Leg roll, and the flare decide"); `07-playtest-rules.md:35` (Rulings, both replacements), `:54` (**Bounds**), `:166` (the *Decided* note), `:278` (the design note's last sentence); `route.yaml:18-29` (`mission_brief_fields` appends), `:31-47` (`bounds`), `:11` (`decided`); `hazards.yaml:57-59` (the comment above `leg_hazards.modifiers`; the mapping unchanged, and the rendered table did not move), `:12` (`decided`); `CONTEXT.md` **Mission Brief** (the bounds sentence); ADR-0009:17; ADR-0014:55; OQ-141:3121 and OQ-167:3535. The bounds match the interim route's envelope at `07:59-61` (D3+3 Legs; Near, Far, Deep by place; no Depot). |
| Opus | m1 | Minor | **Resolved** | 9-41 item 1. `squad-supply.yaml:50-55` (a place is one building, wagon, cache, or the like; a Waypoint scene yields to at most two called searches), `:59-60` (the units found are logged); `04-gear.md:611` (the same two sentences before "The GM spends, adds, or removes units in no other way"); `07-playtest-rules.md:160` (the *It runs on called rolls* bullet). |
| Opus | m2 | Minor | **Resolved** | 9-41 item 2. `01-core-rules.md:28` ("a lost item is one of the roller's, a lost unit is the Squad's, and time or position falls on the situation"); `dice-pool.yaml:131-136` (`together`, with the comment "restored in full by 9-41, item 2"). |
| Opus | m3 | Minor | **Resolved** | 9-41 item 3. `07-playtest-rules.md:276` ("and any other roll a row's own text calls for" ... "A Titan Engagement or a Skirmish a row begins takes its Circumstances as any fight does ... including a step the GM names for a condition that begins during it (decision batch 9, 9-26 and 9-41)"). `hazards.yaml` no longer carries the broad "any roll a Leg Hazard or Night row calls for" sentence (grep: none). |
| Opus | m4 | Minor | **Resolved** | 9-41 item 4. `circumstances.yaml:67-73` (`hazard_rows`, after `reactions`, with the decided comment and the pointer to `hazards.yaml`, `rolls`, `straggler-endure`). |
| Opus | m5 | Minor | **Resolved** | 9-41 item 5. `skirmish.yaml:130-142` (`improvised_act`: "one entry of kind action this list allows ... never a Reaction (Block or Dodge), a roll, a fixed roll, or an option, which rules out reload, release, swap-blade-set, shed-load, and mount-or-dismount", and 9-27's two clauses appended, citing 9-27 and 9-41). |
| Opus | m6 | Minor | **Resolved** | 9-41 item 6. `tools/render/render.py:1260-1262` (`expect_keys(r, set(ROLL_KEYS) | {"changes", "notes", "circumstances"}, ...)`, with the decided comment); `downtime.yaml:140` (`circumstances: never` as a field on `infirmary-roll`; the comment fallback is gone). The render check passes with the key present. |
| Opus | m7 | Minor | **Resolved** | 9-41 item 7. `foes.yaml:153-159` (`gm_rulings.table_made`: "waits for Phase 2: none ships in the first playtest, and no chapter gives the GM the power to add one"); `07-playtest-rules.md:599` (the *Decided* note after the 9-14 note: "the three Foe kinds above are the playtest's whole list"). |
| Opus | m8 | Minor | **Resolved** | 9-41 item 8. `07-playtest-rules.md:691` (**The GM's trigger** bullet after **A first kill**, pointing at Chapter 3, section 3.12, and carrying 9-34's "never called for an outcome a called roll's stakes named"); `:717` (the design note's last sentence: the probe makes no GM-called Fear Roll); `fear-rolls.yaml:86-87` (`gm-horror.called`: "A Skirmish is outside a Titan Engagement, so the trigger is open there"). |
| Opus | m9 | Minor | **Resolved** | 9-41 item 9. `04-gear.md:322` (last sentence: "The GM states the soldier's current Health with those stakes (Chapter 1, section 1.1, item 2; decision batch 9, 9-35)"); `falls.yaml:129-130` (`after_the_fall.gm`, the same sentence with the `health.yaml` pointer). |
| Astra | M1 | Major | **Resolved** | 9-40 applied verbatim. `legs.yaml:245-252` (`night_camp.at_waypoint`: the camp's Waypoint, its kind sets the Anchor Rating, the night hazard still reads the last ridden Leg's band), `:282-286` (`night_camp.retreat`: a retreat at a camp already at the fallback Waypoint moves it no further; no new camp roll, ration, or night hazard; the one Night Camp already counted; the queued Leg unchanged, ridden first at Steady); `hazards.yaml:187` (`distance_band` comment), `:190-195` (`anchor_rating`), `:197-201` (`retreat`); `07-playtest-rules.md:163` (*A retreat* bullet: "which is then the camp's Waypoint for the night hazard and for a Titan Engagement it begins"), `:231` (step 4, the camp's Waypoint and "The Distance Band added above is still the day's last ridden Leg's, whatever the retreat"), `:232` (the second-retreat sentence). |
| Astra | m1 | Minor | **Resolved** (already fixed, verified) | `docs/adr/0016-content-beyond-canon-follows-the-charter.md:12` cites "ADR-0024 limits 9 and 12"; `docs/adr/0019-titan-attacks-are-rolled-and-the-reaction-cancels.md:5` closes on "ADR-0024, limit 8". Neither file contains `ADR-0003`. |

## What the round 2 fixes opened (task check 2)

Walked, and found sound: the called roll in a fight against the Skirmish's closed action list (`07:511-524`: the list stays closed to entries, and the called roll and the improvised act are the two paragraphs after it, each gated), the turn order (`07:504`, `skirmish.yaml:104-106`: an action on the soldier's card), Help (`07:526` and the `called-roll` row at `07:709` agree word for word with `01:290` and `dice-pool.yaml:172-175`), retries (`01:47`, `07:709` retry column, `dice-pool.yaml:230-232`), Reaction timing (`07:543`, `circumstances.yaml:60-66`: a called roll is an action on the soldier's turn and no called roll stands in for a Reaction, 9-36 item 6), and the stakes menu (`01:36-45` as read in a fight; a fall's band at `04:330`). The Nape strike model against Openings (`titan-harm.yaml:113-131`: each Opening records its creator, a creator never spends their own, no limit), Relentless (m4 below), and Nape Depth (nothing lowers it). The Brief's bounds against the interim route (`07:59-61`), Depots (`07:90`, `route.yaml:20-21`, one at most and never the gate), Standard Issue (`07:95-97`, unchanged), and the six hazard modifiers (`hazards.yaml:60-66`: band, camps, Post, Pace, Leg roll, flare; the route sets the first, third, and fourth as written). The height cap against knock loose, letting go, and a release from a lift (all three still read the steps, as 9-38 item 3 says). The fallback camp against the Night procedure (`07:228-233`: camp roll, ration, Camp Relief, night hazard, a day passes, all made at the fallback camp as an ordinary Night Camp), rations (one ration at step 2, none on a second retreat), and the queued repeat (`07:163`, `:232`, `legs.yaml:284-286`).

The Minors below are what the walk turned up.

## Minor findings

### `docs/rules/02-character-creation.md` section 2.9, with `data/character/action-catalog.yaml`

- **m1. Minor: two sentences that predate 9-36 still give Help on a called roll "as the GM rules" without the fight carve-out.** *Location:* `02-character-creation.md:619` ("Help on it is as the GM rules (Chapter 1, section 1.8) ... In a fight a called roll is made the same way, for the acts *In a fight* below leaves to it") and `:632` (*Rolls called by attribute*: "A called roll made with an attribute alone is built the same way, with Help as the GM rules"); `action-catalog.yaml:1060` (`no_value_or_no_match`, which is scoped to "Anywhere else" and is fine) and `:1106-1107` (`rolls_called_by_attribute`, the same attribute-alone sentence with no scope). 9-36 said "No other file changes", so this is a residue, not a misapplication; `01:290`, `02:621`, `05:450`, `07:524`, and `dice-pool.yaml:172-175` all say Help in a fight is the fight's Help with no cost the GM names. *Scenario:* round 2, Private Wendt hauls the granary doors shut, a called roll on Strength alone. The GM reads *Rolls called by attribute* ("Help as the GM rules"), lets a comrade at Distant Help him at On Body, and names a cost of time for it; 9-36 and ADR-0024 limit 7 forbid both. *Fix:* in `02:632` and `action-catalog.yaml:1107`, read "with Help as the GM rules outside a fight and as the fight's Help in one (Chapter 1, section 1.8)"; in `02:619`, read "In a fight a called roll is made the same way, except that Help on it is the fight's Help, for the acts *In a fight* below leaves to it".

### `CONTEXT.md`, **Help**

- **m2. Minor: the glossary's Help entry gives a called roll in a Skirmish GM-ruled Help with a time-or-position cost.** *Location:* `CONTEXT.md:168`: "In a Titan Engagement it spends the helper's action and needs the same Position or one step away; outside one, ... on a called roll the GM rules who is present and able, with Help spending nothing unless the GM names a cost of time or position". A Skirmish is outside a Titan Engagement, so the entry reads a called roll in a Skirmish as GM-ruled Help at a nameable cost, against **Called roll** two entries later (`:176`, "Help on it is then the fight's Help") and `07:526`. WP-S6 was asked for **Called roll** and **Mission Brief** only, so this is a gap beside the fix, not a misapplication. *Fix:* in **Help**, read "outside a Titan Engagement and a Skirmish, the rule that calls for the roll states who can Help ... ; in a Skirmish, up to 3 comrades taking part who are not Down, each spending their action".

### `docs/rules/07-playtest-rules.md` section 7.4, with `data/skirmish/skirmish.yaml`

- **m3. Minor: the Skirmish's parenthetical list of what it tracks omits a Foe's roll and the group's Watch.** *Location:* `07:524` and `skirmish.yaml:145-147` ("changes nothing the Skirmish tracks (no Foe's Health, Guard, Grit, or Parley value, no Engaged or Apart, no card)"), the 9-36 bullet's own words, against 9-36 item 1's definition, which also names "a Titan's or Foe's roll or card", and `01:28` and `dice-pool.yaml:141-142`, which say "Foe value". A Foe's Attack Dice are closed by ADR-0024 limit 8 and `07:35` in any case, so nothing here lets a ruling reach them; the gap is that a GM reading the Skirmish list alone might take Attack Dice or Watch as outside "what the Skirmish tracks" and allow a called roll aimed at them. *Scenario:* Private Brandt kicks the lantern over to blind a trooper. No entry of kind `action` models a penalty on a Foe's dice, so the GM reads the parenthetical, finds Attack Dice absent, and allows a called roll whose success would take 1 die off the trooper's next attack. `07:35` and limit 8 forbid the effect, but the parenthetical invited the question. *Fix:* in both places, append ", no Foe's Attack Dice or Watch" to the parenthetical, or replace it with "(no Foe value, Engaged or Apart, or card)".

### `data/engagement/titan-harm.yaml` `nape_strikes.modelled`, with Chapter 5 section 5.4 and `data/character/talents.yaml`

- **m4. Minor: 9-39's "Relentless adds 1 more" does not carry Relentless's own "with at least 1 success" condition.** *Location:* `titan-harm.yaml:105-110`, `05:448`, `02:628`, and `action-catalog.yaml:1088-1092` all read "each success creates 1 Opening, and Relentless adds 1 more"; `talents.yaml:378` triggers Relentless only on a strike that "falls short ... with at least 1 success". By simulation the review's build rolls 0 successes 6.4% of the time (a Rookie with no Bonus Dice more often). *Scenario:* a Slayer with Relentless levers the cornice onto the Nape, rolls 0 successes, and the table reads `05:448` as 0 plus 1 Opening; `talents.yaml` gives none. *Fix:* in `titan-harm.yaml` `modelled` and `05:448`, read "Relentless adds 1 more where its trigger is met (at least 1 success)", which is one clause and matches `falls_short`.

- **m5. Minor: the hooked-by-strike flag is in the YAML's `modelled` and in neither chapter.** *Location:* `titan-harm.yaml:108-109` ("and it sets the flag (flag)") against `05:448` and `02:628`, which name the Openings only. 9-39's Chapter 5 bullet was applied as written, so this is the decision's own omission. The chapter's general improvised-act rule ("produces only the model entry's effect, at that entry's size", `05:440`) does not obviously cover a flag a real strike sets "whatever its result" (`titan-harm.yaml:101`). *Scenario:* the cornice act rolls 0 successes. By the YAML the Titan's hooked-by-strike flag is set and the Attention Ladder reads it on the next card; by the chapter the GM has no reason to set it. *Fix:* append to `05:448` "and it sets the hooked-by-strike flag, as any Nape strike does (`titan-harm.yaml`, `flag`)".

### `docs/rules/05-titan-engagement.md` section 5.4, *A called roll in a Titan Engagement*

- **m6. Minor: the paragraph says a called roll's success and its time-or-position cost never give a Position, but not that a staked fall still lands by the fall rule.** *Location:* `05:450` ("never a Position or an effect an entry has") with `04:331` (step 2 still finds the reference Titan for "where the fall lands") and `positions.yaml:164-167` (`falls_land`: from On Body or Blind Spot the soldier holds In Reach once the fall is resolved). *Scenario:* Private Brandt at Blind Spot cuts a signboard's strap so it swings clear of a comrade's line, the decision's own example; the GM stakes a high fall. She fails, falls high (D6+2), and by `falls_land` now holds In Reach. A GM who has just read that a called roll "never reaches" a Position may hold her at Blind Spot, or may wonder whether the stake was legal. The fall's landing is the fall rule's, not the ruling's, and the text should say so. *Fix:* after "the Height steps of Chapter 4 do not raise it" insert ", and it lands as section 5.2 gives (`positions.yaml`, `falls_land`)".

### `data/expedition/legs.yaml` `night_camp.at_waypoint`, with Chapter 7 section 7.1 and `data/expedition/route.yaml`

- **m7. Minor: a fallback camp at the departure gate has no Waypoint kind to read.** *Location:* `legs.yaml:246-250` and `07:231` (the Anchor Rating comes "from the kind of that Waypoint"); `route.yaml:61` (`gate`, `anchor_rating: null`) and `07:87` (the gate's kind is "none; roll it on the setup table", and it is "never rolled; always the route's last Waypoint"); `07:47-49` (the route's Waypoints begin with the first Leg's destination, so the point the first Leg starts from is a gate that is not on the list). 9-40 opened this: before it the Night table read "the Waypoint the day's last Leg reached", which is never the gate. *Scenario:* day 1. Leg 1 ends at Waypoint A, a Waypoint-scene Titan Engagement there becomes a retreat, Leg 1 is ridden again as the day's second, a second scene there becomes a second retreat, and no slot remains (no Hard Ride ordered). The Squad camps at the gate it rode out of. The night hazard rolls the Abnormal, and the Anchor Rating is the kind of a Waypoint that has none. The setup-table fallback exists for a gate, but nothing says the departure gate is a Waypoint, and a Survey Corps Squad at its own gate would be inside the Walls. Rare, and worth one sentence. *Fix:* in `legs.yaml` `at_waypoint` and `07:231`, add "A camp at the gate the Expedition left rolls its Anchor Rating on the setup table, as the Waypoint kinds table gives for a gate"; or, if the fiction should win, say the Expedition ends there as at the route's last gate, which is a 9-33 reading and needs a decider.

### `docs/rules/PROGRESS.md`

- **m8. Minor: the GM judgment packages table does not list every package of this change, and its process line is one round behind.** *Location:* `PROGRESS.md:75-91` (the *GM judgment packages* table) has rows for WP-G0 to WP-G5, 9-21 to 9-24, Review (round 1 only), WP-M4, WP-R6, WP-S6, and WP-P3, and none for WP-R1 to WP-R5 or WP-S1 to WP-S5, which the task file names; those packages appear only inside the chapter rows at lines 13 to 19 (R1, WP-R2, R3, WP-R4, WP-S1 to WP-S5). The Review row stops at round 1, so round 2 (two reviews, 15 findings, the round 2 fix plan, 9-36 to 9-41) and this round are unrecorded; line 9 still says "one GM judgment review (Opus and Codex gpt-6-astra) runs before the packet's Version 3". *Fix:* add one row each for WP-R1 to WP-R5 and WP-S1 to WP-S5 (Done, with the items each applied, as the chapter rows already state), extend the Review row to rounds 2 and 3 with their files and counts, and read line 9 as "the GM judgment review runs in up to three rounds".

## Prioritised fix list

1. **m1 and m2** together, since both are the same residue: scope "Help as the GM rules" to outside a fight in `02:619`, `02:632`, `action-catalog.yaml:1107`, and `CONTEXT.md` **Help**.
2. **m4 and m5:** one clause each in `titan-harm.yaml` `modelled` and `05:448` (Relentless's at-least-1 condition; the flag), with `02:628` matched.
3. **m3:** the Skirmish parenthetical in `07:524` and `skirmish.yaml:145-147`.
4. **m6:** one clause in `05:450`.
5. **m7:** one sentence in `legs.yaml` `at_waypoint` and `07:231`, or a decider if the Expedition should end at the gate.
6. **m8:** the PROGRESS.md rows.

Routing: m1 to m6 and m8 are drafting fixes inside batch 9's own limits; none touches a closed number or a guarded field. m7 is a drafting fix if the setup-table reading is taken, and a decider's if the gate ends the Expedition.
